====== 지옥에서 온 GIT =====

from Youtube "지옥에서 온 GIT" 
uploaded in FEB 2017~
- by 이고잉
 
 
1. 버전 관리 시스템
- Storage cloud
- version management, not by changing the file name
- backup,recovery, collaboration
- CVS, SVN, GIT ..
- 핵심적이 부분: 변경사항 관리 가능 - 본질(보편적이고, 쉽다)
- 혁신적이 부분: SVN이 CVS, GIT이 SVN의 아성을 무너뜨릴 수 있었던 이유 - 혁신(편리, 어렵고, 불필요할수도)

- git is incredibly complex (DROP BOX, Google Drive부터 사용하기를 권장)
- 그럼에도 사용하는 이유? because real life is more hell than GIT
- complexity of GIT + complexity of code = comlexity of project
- 따라서, git으 효용은 complexity of code에 달려있음. 


6. Mac에서 git init하기
terminal
>cd Documents: Docments 안에 프로젝트 폴더 만들어라. 
>ls -al
>cd gitfth
>git init
>ls -al
>

7.
>vim f1.txt : vim이라는 프로그램으로 f1.txt라는 파일 편집한다는 뜻
>i : insert 모드


40. PUSH & PULL (My server)
두대의 컴퓨터 가정(home, offie)
<home>
>git remote -v

<office>
>git remote -v
두대가 똑같은 원격저장소에 연결돼 있음 확인

<home>
>git commit -m ""
>git push

<office>
>git pull 원격저장소의 내용 로컬 저장소로 땡겨오는 것
>git log
작업
>git push

<home>
>git pull 안하고 그냥 작업?
작업후
>git push
에러메세지(원격저장소의 것을 pull하여 병합하고 충돌하는 일을 지역 저장소에서 한다음에 원격 저장소로 올리라는
>git log
merge commit이 만들어진 것을 볼 수 있음
>git push
